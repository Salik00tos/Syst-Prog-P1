// Header guard: prevents including this "outline" multiple times
#ifndef GREET_H
#define GREET_H

// This is just a declaration (like a section title in an outline)
// It tells other files: "This function exists somewhere"
void greet(const char *name);

#endif